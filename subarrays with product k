int Solution::solve(vector<int> &A, int B) {
    unordered_map<int,int>mpp;
    mpp[0] = 1;
    int xori = 0, count = 0;
    for(int i = 0;i<A.size();i++){
        xori ^= A[i];
        int x = xori ^ B;
        count += mpp[x];
        mpp[xori]++;
    }
    return count;
}
